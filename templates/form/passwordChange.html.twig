{% set form = passwordChangeForm %}
{% form_theme form 'form/themes/default.html.twig' %}

{{ form_start(form, {
	'attr': {
		'novalidate': true,
		'class': 'base',
	},
}) }}

{{ form_row(form['password']) }}

{{ form_widget(form['submit'], {
	'attr': {
		'class': 'btn btn-secondary',
	},
}) }}

{{ form_end(form) }}
